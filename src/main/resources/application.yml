spring:
  application:
    name: employees-management

  datasource:
    url: jdbc:mysql://localhost:3306/company
    username: root
    password: 7953

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false

server:
  port: 8000

---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    username: root
    password: linux-dev
