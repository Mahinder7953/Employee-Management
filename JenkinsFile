pipeline {
    agent any
    tools { 
    jdk 'java-21' 
    maven 'Maven-3.9.12'
    }
    environment {
        APP_NAME = "employee-service"
    }    

    stages {
        
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Mahinder7953/Employee-Management.git'
            }
        }
        
        stage('Build') {
            steps {
                    sh 'java -version'
                    sh 'javac -version'
                    sh ' /opt/maven/bin/mvn clean package -DskipTests'
        
            }
        }
        
        stage('Deploy') {
            steps {
                sh "sudo cp target/*.jar /apps/apps-root/Employees-Management/${APP_NAME}.jar"
            }
        }
        
    }
}
